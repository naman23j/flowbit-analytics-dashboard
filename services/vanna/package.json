{
  "name": "vanna-ai-service",
  "version": "1.0.0",
  "description": "Vanna AI service for natural language to SQL conversion using Groq",
  "scripts": {
    "dev": "python -m uvicorn main:app --reload --port 8000",
    "start": "python -m uvicorn main:app --host 0.0.0.0 --port 8000"
  },
  "dependencies": {
    "react-chartjs-2": "^5.3.1"
  }
}
